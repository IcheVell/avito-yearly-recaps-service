import { ProfilePage } from '../pages/ProfilePage/ProfilePage';

/**
 * Корневой компонент.
 * Пока роутов нет, поэтому сразу открываем страницу профилей.
 */
export function App() {
  return <ProfilePage />;
}
